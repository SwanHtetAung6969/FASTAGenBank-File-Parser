Lets the user upload a file
- Detects whether the file is FASTA or GenBank
- Parses FASTA headers and sequences
- Parses GenBank LOCUS and FEATURES sections
- Displays the parsed information in a readable format
